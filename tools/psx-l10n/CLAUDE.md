# 창세기전2 PS1 프로토타입 한글화 — 작업 인수인계

이 폴더는 PS1용 『창세기전2』 미발매 프로토타입(일본판)의 한글화 프로젝트다.
Claude Code 세션은 이 문서를 먼저 읽고 이어서 진행한다.

## 배경

- 창세기전2는 1996년 소프트맥스의 한국 PC 게임. PS1 이식판은 일본 퍼블리셔 VIP와
  준비되어 정식 시리얼까지 받았으나 VIP 파산으로 **발매되지 않았다**.
- 사용자는 당시 소프트맥스에서 이 빌드를 QA 테스트했고, 그 테스트 빌드 이미지를
  보관하고 있다. PC판과는 **대사가 다른 별개 스크립트**다.
- 이 빌드에 **한국어는 한 줄도 없다.** 일본어로 완전히 교체된 상태다. 따라서 이
  프로젝트는 **일본어 → 한국어 완역**이며, 인명·지명은 창세기전 한국어 정식 표기를
  따른다.

## 디스크 이미지 위치 (사용자 PC)

```
D:\g드라이브\창세기전\창세기전\창세기전2-1.BIN   (디스크 1, 677,580,624 bytes)
D:\g드라이브\창세기전\창세기전\창세기전2-2.BIN   (디스크 2, 652,216,656 bytes)
```

WSL(우분투)에서 작업할 때의 경로:

```
/mnt/d/g드라이브/창세기전/창세기전/창세기전2-1.BIN
/mnt/d/g드라이브/창세기전/창세기전/창세기전2-2.BIN
```

같은 폴더에 있는 다른 파일(2026-09-20 `ls`로 확인, 경로 이상 없음):

- `창세기전2-1.iso` (590,002,176), `창세기전2-2.iso` (567,916,544) — 2048바이트/섹터로
  변환한 사본. **작업은 `.BIN`(raw)으로 한다.** 리빌드 검증용 비교 대상으로만 쓴다.
- `창세기전2-2.CUE` — 디스크2 큐시트(`MODE2/2352`, 파일명 인코딩 깨짐). 디스크1 큐는 없음.
- `창세1-1/SLPS02311/EBOOT.PBP`, `창세1-1/SLPS02312/EBOOT.PBP` — PSP용 변환본. 무시.

`/mnt/d`는 Windows의 D: 드라이브다. 파일명은 먼저 `ls`로 확인한다. `/mnt/d` 읽기는
WSL 파일시스템보다 느리므로, 추출 결과(`ex/`)는 리포 안(리눅스 쪽)에 둔다.

구글 드라이브 동기화 폴더일 수 있다. 작업 산출물(추출 파일 등)은 이 폴더가
아니라 리포 안의 `ex/` 같은 곳에 둔다. `.gitignore`가 막고 있으니 커밋되지 않는다.

## 이미 확인된 구조 (두 디스크 모두 검사 완료)

| 항목 | 내용 |
|---|---|
| 형식 | MODE2/FORM1 raw, 2352 bytes/sector, 데이터 오프셋 24 |
| 시리얼 | 디스크1 `SLPS_023.11`, 디스크2 `SLPS_023.12` |
| `G2DATA1.DAT` | 76,509,184 bytes. **일본어 스크립트 전체가 여기.** Shift-JIS 평문, 7,321개 덩어리 |
| `G2DATA2.DAT` | 274,221,154 bytes. 텍스트 0건 → 그래픽·사운드 |
| `SLPS_023.11` | 1,138,688 bytes. 실행파일. 시스템 메시지 133건. **디스크 1에만 있음** |
| `XA/`, `XA3/` | STR 동영상·XA 음성. 디스크 간에 다른 건 이것뿐 |
| 두 디스크 관계 | `G2DATA1.DAT`, `G2DATA2.DAT`는 파일 내 오프셋까지 **완전히 동일** |

결론: **`G2DATA1.DAT` 하나를 한글화해서 두 디스크에 같이 넣으면 된다.** 코드 수정은
디스크 1의 실행파일 하나.

알려진 좌표:
- `G2DATA1.DAT+0x75874C` — 90글자짜리 대사 덩어리. 제어 코드 분석 출발점
- `G2DATA1.DAT+0x33ED697` 부근 — `0xB8` 채움 데이터 (한글 오탐 원인, 무시)
- 디스크2 절대 오프셋 `0x0A7F76C0` 부근 — 가타카나 인명 테이블, 고정 길이 레코드로 보임

## 2026-09-20 세션에서 확인한 것 (1~5단계 완료)

추출은 `ex/`에 되어 있다(디스크 1 전체 30개 파일). 아래는 전부 `ex/` 기준.

### G2DATA1.DAT 인덱스 — EXE 안에 있다

- EXE 파일 오프셋 `0xC1AF8~0xC4448` (RAM `0x800D12F8`), **u32 바이트 오프셋 배열, `FF FF FF FF`로
  구분된 60개 그룹, 총 2,523 엔트리**. 그룹 사이에 수 KB~185KB 공백(그룹 마지막 엔트리의 실제 크기).
- `ex/g2data1_index.tsv` (group, idx, offset, size)로 저장해 둠. 없으면 이 문서의 좌표로 재생성.
- G2DATA1.DAT 자체에는 헤더가 없다. 파일 선두가 바로 캐릭터 테이블.

### 텍스트가 있는 곳

| 위치 (G2DATA1) | 내용 | 구조 |
|---|---|---|
| `0x0` ~ 약 `0x8000` | **캐릭터 테이블** (イオリン=이올린, レイシッド, デュラン…) | 256바이트 레코드: +0 짧은 이름(24B), +24 긴 이름(24B), +0x78 스탯 |
| `0xC464`~ | 직업 설명 | NUL 종결, 4바이트 정렬 |
| `0xE7AC`~ | **아이템 테이블** (S.T.S, 魔法の盾, 火の盾, レッドネックレス…) | 68바이트 레코드, +0 이름 |
| `0xEFE8`~ | 아이템 설명 | NUL 종결 |
| `0x11EBC`~ | 마을 NPC 대사 | NUL 종결 |
| `0x8C0000`~`0x1810000` | **스토리 대사 본체** (7,679 문자열) | 아래 "텍스트 섹션" 형식 |
| EXE `SLPS_023.11` | 시스템 메시지, **마법 이름**(ダーク・ゾーン…), 메뉴(ターン終了/ＴＰタメ/ステータス), 선택지 | NUL 종결 SJIS, RAM 포인터로 참조. `ex/SLPS_023.11.tsv` |

### 텍스트 섹션 형식 (스토리 대사) — 포인터 테이블 찾음

각 스크립트 블록(인덱스 엔트리, 예: 그룹 `0xC1F1C` #340 = `[0x744F48, 0x780238)`) 안에
텍스트 섹션이 들어 있고, 형식은:

```
u32 count          ; 레코드 수
u32 size           ; 섹션 전체 크기 (count 필드부터 마지막 레코드 끝까지)
u32 offs[count]    ; 각 레코드의 상대 오프셋 (기준 = offs 배열 바로 뒤 = 첫 레코드)
레코드 × count:
  u32 len          ; 본문 바이트 수 (NUL 제외)
  u8  text[len]    ; Shift-JIS
  u8  0            ; NUL, 이후 4바이트 정렬 패딩
```

검증: 0x744F48 블록의 섹션(블록 상대 `0x12A20`) count=45, offs 45/45 일치, 주 대사 구역의
`/5` 문자열 703개 모두 len 필드 == 실제 길이. **레코드 길이가 바뀌면 len·offs·size를
재계산하면 된다.** 단, 섹션 뒤에 다른 데이터가 이어지므로 섹션이 커지면 블록 전체와
EXE 인덱스(뒤 엔트리 전부)를 밀어야 한다. 블록 안에서 이 섹션을 가리키는 값은 아직
못 찾았다(u32/u16, 절대/상대 모두 0건) → 블록 헤더(선두 u16 ID 열) 해석이 남은 과제.

제어 코드는 **ASCII 이스케이프**: `/5`(화자명 시작, 색), `/1`(본문 복귀), `/n`(줄바꿈),
`/s3`(선택지 구분?). 바이너리 제어 코드 없음.

### 폰트 — EXE 안, 표준 TIM 아님 (timtool 결과 7개는 전부 오탐)

| 항목 | 값 |
|---|---|
| 형식 | **1bpp, 16×13 픽셀, 글리프당 26바이트**(13행 × u16 big-endian 비트열) |
| 비한자 | RAM `0x800DFBDC` (파일 `0xD03DC`), 524자 — 기호·숫자·라틴·히라가나·가타카나·그리스·키릴·선문자 |
| 1수준 한자 | RAM `0x800E3114`, 2,965자 (JIS 순서 亜唖娃…) |
| 2수준 한자 | RAM `0x800F5E38` ~ `0x8010B684`, 3,390자 |
| 합계 | 6,879 글리프, 178,854바이트 |
| 렌더링 | **가변폭**. 글리프의 좌우 빈 열을 찾아 실제 폭 + 4px 로 진행. 빈 글리프는 8px |

렌더러 코드:
- `0x80019A50` **SJIS → 글리프 인덱스 + 그리기**. a0=선두 바이트, a1=후속 바이트.
  세 구간(0x8140~0x84BE 비한자 / 0x889F~0x9872 한자1 / 0x989F~0xEAA4 한자2)을
  (빼기, 더하기) u16 쌍 리맵 테이블 `0x800DFA7C`(19쌍) / `0x800DFAC8`(32쌍) /
  `0x800DFB48`(37쌍)로 압축 인덱스로 바꾸고, `인덱스×26`을 폰트 베이스에 더한다.
- 폰트 베이스 포인터 3개는 `gp+0x438/0x43C/0x440` (`gp=0x80124E08` → `0x80125240..48`),
  `0x80019404`에서 상수로 설정.
- 글리프는 `0x80153F90 + (a2>>1)`의 8bpp 소프트웨어 버퍼(행 stride 0x80)에 색
  `gp+0x444/0x445`로 찍힌다. 그 뒤 VRAM으로 올리는 코드는 아직 안 봤다.

확인 명령: `python3 tileview.py ex/SLPS_023.11 0xD03DC out.png --bpp 1 --tile 16x13 --count 524`

### 한글 폰트 — 결정됨: Galmuri11 (2026-09-20)

사용자가 **Galmuri11**(quiple, SIL OFL 1.1, 12px/한글 11×11, BDF 배포)로 확정했다.
전략은 **코드 치환**: 한글을 쓰지 않는 JIS 한자 슬롯에 1:1 배정하고, 그 슬롯의 26바이트
글리프만 Galmuri11 비트맵으로 교체한다. 렌더러는 수정하지 않는다.

- 폰트 파일: `fonts/Galmuri11.bdf`, `fonts/Galmuri11-Bold.bdf`, `fonts/Galmuri11-Condensed.bdf`
  (`fonts/`는 gitignore. 없으면
  `curl -L -o fonts/Galmuri11.bdf https://raw.githubusercontent.com/quiple/galmuri/main/dist/Galmuri11.bdf`)
- 변환: `python3 hangulfont.py build fonts/Galmuri11.bdf fonts/galmuri11_ksx1001.bin --map fonts/galmuri11_ksx1001.tsv --png sheet.png`
  → KS X 1001 2,350자, 잘림 0, 진행 폭 14~15px (원본 가나 13~15px과 동급).
- 배치: `--baseline 11 --dx 1` (기본값). 한글 본체가 슬롯 1~11행·1~11열 → 원본 한자(0~12행)와 중심 일치.
  BDF의 `FONT_ASCENT 14`는 슬롯보다 커서 쓰지 않는다.
- Regular이 원본 획 굵기(1px)와 같다. Bold는 CRT/컴포지트 실기 검증에서 가독성이 부족할 때의
  대안(`fonts/preview_galmuri11_*.png` 비교 참고).
- 미리보기: `python3 hangulfont.py preview fonts/Galmuri11.bdf "문장" out.png --scale 3 --game-exe ex/SLPS_023.11`
  (게임 규칙 = 글리프 실폭 + 4px, 빈 글리프 8px 로 문장을 찍고 아래 줄에 원본 가나를 같은 규칙으로 찍음).

남은 폰트 작업: (a) 한글 2,350자(+한글 문장부호) ↔ JIS 2수준 한자 슬롯(0x989F~, 3,390개) 대응표
확정 — 번역에 실제로 쓰는 일본어 한자가 있으면 그 슬롯은 피한다, (b) EXE `0x800F5E38`부터 해당
슬롯에 글리프 블롭 덮어쓰기, (c) 번역 TSV → 대응표 인코딩.

## 도구 (이 폴더, 의존성 없는 Python 3)

| 도구 | 용도 |
|---|---|
| `psxtool.py identify/ls/extract/scan-text` | 디스크 이미지 식별·추출·언어 판정 |
| `psxsniff.py triage/archive` | 파일 정체 판정, 아카이브 오프셋 테이블 분석·언팩 |
| `timtool.py scan/extract` | TIM 이미지 탐색, PNG 변환 (폰트 찾기) |
| `sjisdump.py map/dump/peek` | Shift-JIS 문자열 지도·덤프·16진 보기 |
| `tileview.py` | 헤더 없는 원시 비트맵/타일을 PNG로 (폰트 확인용, `--bpp 1 --tile 16x13`) |
| `mipsdis.py` | PS-X EXE 미니 디스어셈블러 (`--find-ref`, `--calls`, lui 쌍 주소 주석) |
| `hangulfont.py info/build/preview/patch/shot` | BDF → 게임 폰트 슬롯(16×13, 26B) 변환·EXE 패치·게임 경로 렌더 |
| `g2text.py sections/dump/chars` | 텍스트 섹션 탐색·덤프, 실제 쓰이는 문자 집계 |
| `koremap.py build/encode/decode/check` | 한글 ↔ 게임 코드 대응표, 인코딩·검증 |
| `g2patch.py table/exestr/apply` | 고정 레코드·NUL 문자열 번역 TSV 생성과 제자리 적용 |

사용법은 `README.md`. 새 도구를 만들 때도 같은 원칙: 단일 파일, 표준 라이브러리만,
한국어 도움말, 합성 데이터로 검증 후 커밋.

## 작업 순서

1~5는 끝났다(위 섹션). 남은 것:

6. **한글 폰트**: Galmuri11 + 코드 치환으로 확정, 변환 도구(`hangulfont.py`) 완료. 남은 것:
   (a) 한글↔JIS 슬롯 대응표 생성 도구, (b) EXE 폰트 패치.
   추가로 볼 것: `0x80153F90` 버퍼를 VRAM으로 올리는 코드(글자 캐시 크기 제한 여부),
   메뉴/이름처럼 EXE·테이블에 고정 길이로 박힌 문자열의 폭 제한.
7. **재삽입 파이프라인**: 번역 TSV → 대응표 인코딩 → 텍스트 섹션(len/offs/size) 재조립 →
   블록·EXE 인덱스 재계산 → `G2DATA1.DAT` 재조립. 먼저 캐릭터/아이템 테이블(고정 길이라
   인덱스 재계산 불필요)과 EXE 문자열로 화면 검증.
8. **리빌드**: `mkpsxiso`로 ISO 재생성, 두 디스크 모두. xdelta 패치로 배포.
9. **검증**: DuckStation으로 실행. 사용자가 화면을 확인한다.

## 2026-09-20 2차 세션 — 한글 출력까지 뚫림

**화면에 한글이 나오는 것까지 확인했다.** 에뮬레이터 실행 전 단계로, 게임 렌더러와
똑같은 경로(코드 → 리맵 테이블 → 글리프 26바이트)로 그려 검증했다
(`hangulfont.py shot`). 같은 방법으로 원본 일본어를 그리면 원본 화면과 일치한다.

### 대응표 — JIS 2수준 한자 슬롯에 한글

`g2text.py chars` 로 **게임이 실제로 쓰는 문자**를 셌다. 2수준 한자는 3,390 슬롯 중
**71자만** 쓰인다(僉僭儼儻囘冓剄剔劔剱哭嗤…). 나머지에 한글을 넣으면 번역 안 한
일본어도 그대로 나온다.

- 집계는 **NUL 종결 + 전체 유효 SJIS + 가나 비율 25% 이상**을 요구한다. 바이트쌍만 세면
  그래픽 데이터가 2수준 한자 1,387개로 오탐된다(실제 71개).
- `koremap.py build` → `ex/koremap.tsv`: KS X 1001 한글 2,350자 + `…·` = 2,352자를
  코드 `0x989F~0xE579`(글리프 인덱스 0~2407)에 배정. **여유 슬롯 967개**.
- ASCII는 게임이 `0x80019514`에서 전각으로 바꿔 찍는다(`.`→`．`, `,`→`，`, `A`→`Ａ`).
  한국어에도 맞으므로 그대로 쓴다. 제어 코드 `/5 /1 /n /s` 는 텍스트 엔진이 먼저 먹는다.
  → **`/`·`n`·`s`·숫자를 대응표에 넣으면 제어 코드가 깨진다. 넣지 말 것.**

### 폰트 패치

`hangulfont.py patch fonts/Galmuri11.bdf ex/koremap.tsv ex/SLPS_023.11 -o ex/SLPS_023.11.ko`

EXE 크기 그대로(코드 이동 없음), `0x800F5E38`부터의 2수준 슬롯만 덮어쓴다. 쓰이는 71자
슬롯은 건드리지 않는다(확인 완료).

### 텍스트가 들어가는 곳 — 포인터 재계산 없이

| 대상 | 위치 | 구조 | 개수 |
|---|---|---|---|
| 캐릭터 이름 | `0x0` | 256B 레코드, 이름 24B ×2(짧은/긴) | 38 |
| 아이템 이름 | `0xD008`~`0xEFE8` | 68B 레코드, 이름 30B, +30 분류 바이트로 경계 판정 | 120 |
| EXE 문자열 | `SLPS_023.11` | NUL 종결 + 뒤따르는 0 패딩까지 | 243 |
| **대사 본문** | `G2DATA1.DAT` 전역 | NUL 종결 | **7,731 (약 16만 자)** |

`g2patch.py table/exestr` 로 번역 TSV를 만들고 `apply` 로 제자리에 넣는다. **원문보다
짧거나 같으면** 되고, `ko` 열이 빈 행은 건너뛰므로 번역이 진행되는 대로 계속 다시 돌리면 된다.

산출물: `ex/tr_chars.tsv`, `ex/tr_items.tsv`, `ex/tr_exe.tsv`, `ex/tr_data.tsv`.
**인명·아이템 정식 표기는 사용자 확인이 필요하다** — 현재 채워진 것은 확실한 일부뿐인 가안.

`tr_data.tsv` 7,731행 중 **2,393행은 여유 0바이트**다. 원문보다 길어지는 번역은
텍스트 섹션 재조립(아래 8번)이 필요하다.

### 문자열 찾기 — 오탐 거르는 세 단계

EXE 문자열 스캔은 처음에 2,201개가 나왔는데 그중 1,900여 개가 코드·폰트 데이터가
우연히 Shift-JIS 로 읽힌 오탐이었다(`塘4`, `Ξ:W`, `け澹`…). 세 가지를 겹쳐 243개로 줄였다:

1. **NUL 구간 전체가 유효**해야 한다. 중간의 유효한 2바이트 문자부터 보면 안 된다 —
   앞뒤 코드 바이트까지 검사해야 탈락한다. (덤: ASCII 로 시작하는 문자열이
   `%s %d レベルアップ` 처럼 온전히 잡힌다. 전에는 앞이 잘렸다.)
2. **글자다운 범위**만 허용. 그리스·키릴·괘선은 뺀다. 가나나 한자가 최소 하나.
3. **혼자 떨어져 있는 짧은 것**을 버린다. 진짜 문자열은 풀에 여럿이 붙어 있거나,
   포인터로 참조되거나, 셋 글자 이상이다.

마법명 표처럼 **앞이 NUL 이 아니라 포인터 배열**인 문자열은 1번으로 못 찾는다. 그래서
EXE 안의 u32 중 EXE 를 가리키는 값(5,128개)도 후보 시작점으로 쓴다(PS-X EXE 일 때만).

### 용어집 — 사용자 확정 (2026-09-20)

`ex/tr_chars.tsv`(76), `ex/tr_items.tsv`(120), `ex/tr_exe.tsv`(60/243) 을 사용자가 채웠다.
**인명·아이템은 정식 한국판 명칭이므로 추측으로 고치지 말 것.** 일본어 음만 보면
어긋나 보이는 것들이 있는데 전부 확정이다:

| 일본판 | 정식 한국판 | 메모 |
|---|---|---|
| `グレイ・シュナイダー` | 그레이 스케빈져 | 음역(슈나이더)이 아님 |
| `クリップ` | 크리프 | |
| `サンプラス` | 샘플러스 | |
| `ベネスタ` | 베네스다 | |
| `Chry.ギリアーム` | 크리.그리엄 | `Chry.` = クリサオル(크리사오르) 가문 |
| `ドラウ・プニル` | 드라우푸니르 | |
| `一志鷹` | 일지매 | 鷹=매 를 음차한 일본판 표기 |
| `スタムブリンガー` | 스톰브링거 | 일본판 표기 오류를 바로잡은 것 |

외래어 표기 통일: **`サンダー` → 썬더** (선더 아님). `ジョ` → 죠 (죠엘·죠셉).

검증 결과: 인코딩·길이·폭 전부 통과. 원문 최대 폭(캐릭터 139px, 아이템 130px)을
넘는 번역 없음. 같은 원문에 다른 번역 없음.

남은 것: `tr_exe.tsv` 183행, `tr_data.tsv` 7,731행.

### BIN 되넣기 — ISO 재생성 불필요

패치가 **같은 크기**라서 `mkpsxiso` 없이 해당 파일이 쓰던 섹터만 갈아끼우면 된다
(`binject.py`). MODE2/FORM1 의 EDC(2072~2075)와 ECC(2076~2351)를 다시 계산하며,
`binject.py verify` 로 **원본 섹터를 재계산해 원본 값과 일치**하는지 먼저 확인했다.

```
python3 binject.py verify "원본.BIN"                      # 알고리즘 검증
python3 binject.py inject "원본.BIN" 출력.bin \
    -f G2DATA1.DAT=ex/G2DATA1.ko.DAT -f SLPS_023.11=ex/SLPS_023.11.ko
```

바뀐 섹터만 쓰므로 xdelta 패치가 작다. 디스크 2는 `G2DATA1.DAT` 만 같은 방법으로
넣으면 된다(EXE 는 디스크 1에만 있다).

### 검증 환경 (사용자 PC)

- DuckStation: `C:\Users\tbfl7\AppData\Local\Programs\DuckStation\duckstation-qt-x64-ReleaseLTCG.exe`
- BIOS: 같은 폴더 `bios\` — `SCPH5500.BIN`(일본판)을 쓴다
- 작업본: `C:\Users\tbfl7\g2ko\G2KO-1.bin` + `.cue`.
  **D: 는 구글 드라이브 동기화 폴더라 산출물을 두지 않는다.**

### 다음

1. `ex/tr_*.tsv` 의 `ko` 열 채우기 — 인명·지명은 사용자(당시 QA) 확인 필요.
   용어집(인명·지명·마법·아이템 508행)을 먼저 확정하고, 그 다음 대사 7,731행.
2. 여유 0바이트 행(2,393개)을 위한 텍스트 섹션 재조립 + EXE 인덱스 재계산
3. `mkpsxiso` 리빌드, xdelta 패치, DuckStation 검증

